# Changelog

Формат версии: `X.YY.ZZZ`. Патч (`ZZZ`) увеличивается на 1 при каждой доработке; старшие номера (`X`, `YY`) меняются только по явной команде пользователя.

## 0.00.021 — индикатор метаданных, ускорение старта HLS-прослушивания, прослушивание остановленных каналов

- **Индикатор «тишины» на карточке канала переосмыслен как индикатор здоровья метаданных.** Раньше это была грубая оценка «есть ли сейчас сигнал» по мгновенному True Peak (порог -50 дБ) — независимая от статуса записи, из-за чего при остановленной записи (уровень сбрасывается в -∞) индикатор всегда показывал красный, что вводило в заблуждение. Теперь: серый — если запись не идёт; красный — только если для канала настроена ссылка на метаданные, но они не определяются (после 30-секундного периода ожидания, покрывающего худший случай HLS-обнаружения с повторными попытками); зелёный — иначе (в том числе если метаданные вообще не настроены — проверять нечего). `IMetadataReader` (Icecast/HLS-ридеры) получил `HasMetadata`, `ChannelEngine` — вычисление `MetadataOk` с опросом раз в 500 мс, `EngineStatus.MetadataOk` (`bool?`) прокинут в UI.
- **Ускорен старт прослушивания HLS-потоков.** Причина заметной паузы перед стартом (в отличие от Icecast, который начинает воспроизведение сразу): без `-live_start_index -1` HLS-демуксер ffmpeg по умолчанию начинает с самого старого сегмента, ещё остающегося в окне живого плейлиста, а не с текущего живого края — из-за этого приходится «догонять» через всё уже буферизованное, прежде чем пойдёт актуальный звук. Добавлены `-live_start_index -1` (только для HLS) и `-fflags nobuffer` (для обоих типов потоков) в `StreamCapture.BuildFfmpegArgs`. 2 новых теста.
- **Кнопка ▶/⏹ прослушивания теперь доступна и для остановленных каналов** (раньше требовала идущей записи). При нажатии на неактивный канал `AudioPlaybackService` временно запускает его конвейер захвата только для прослушивания и останавливает обратно при остановке воспроизведения — если канал уже писался по-настоящему, запись не прерывается. Также добавлена подчистка состояния, если исходный канал останавливается сам по себе во время прослушивания (раньше кнопка могла «зависнуть» в состоянии «играет»).
- Зазор между шкалами TP L и TP R на панели индикаторов увеличен с 3 до 5 px.

## 0.00.020 — классификация рекламы в метаданных, урезание языковых пакетов публикации

- Публикация (`dotnet publish`) теперь включает только английский (базовый) и русский языковые пакеты сателлитных сборок .NET/ASP.NET Core — `SatelliteResourceLanguages` в `Quince.Service.csproj` ограничен `en;ru`. Раньше в `release/<версия>/` копировались папки на 13 языков (`cs`, `de`, `es`, `fr`, `it`, `ja`, `ko`, `pl`, `pt-BR`, `ru`, `tr`, `zh-Hans`, `zh-Hant`) с локализованными текстами исключений фреймворка — не нужны для этого приложения.
- **Новое: классификация метаданных по рекламным словам.** `app.yaml` получил `ad_keywords` (по умолчанию «Реклама», «Reklama», «Commercial») — если название или исполнитель трека содержит одно из этих слов (без учёта регистра), в CSV-колонку `ElemClass` пишется `C` вместо обычного `M`. Редактируется в диалоге «Настройки приложения», поле «Слова-маркеры рекламы» (через запятую). `MetadataWriter` получил необязательный `Func<IReadOnlyList<string>>`, читающий актуальный список при каждой классификации (а не одну сохранённую на старте канала копию) — изменение списка в настройках применяется к уже запущенным каналам без перезапуска. 7 новых тестов.

## 0.00.019 — прослушивание канала на аудиовыход, правки диалогов и индикаторов

- **Обзор папки сохранения переделан**: раньше сначала пытался открыть системный диалог Windows прямо на сервере (виден только если кликающий физически/по RDP сидит на той же машине), с откатом на встроенный браузер. Теперь — всегда встроенный браузер по файловой системе сервера, без первой бесполезной попытки. Заодно убран неиспользуемый `System.Windows.Forms`/`NativeFolderPicker.cs`/`UseWindowsForms` — он был нужен только для той попытки.
- Горизонтальный индикатор уровня на карточке канала теперь обнуляется при остановке записи (раньше «зависал» на последнем значении).
- **`file_duration_seconds` → `file_duration_minutes`**: длительность файла записи теперь задаётся в минутах, а не в секундах — так нагляднее для типичных значений (10–60 минут). Старые конфиги с `file_duration_seconds` продолжают загружаться корректно (автоматический пересчёт в минуты при отсутствии нового поля), пересохранение канала запишет уже новое поле.
- Расстояние между карточками каналов увеличено до 24px.
- Индикатор метаданных на карточке канала — с голубого на фиолетовый.
- В панели индикаторов убран текст «макс:» у кнопки пикового максимума — остаётся только число. Шкала `-∞..0 LU` дополнена мелкими засечками через каждый 1 LU (крупные подписанные — как раньше, через 6).
- **Диалог редактирования канала больше не закрывается кликом по фону** — только кнопками «Сохранить»/«Отмена». Раньше клик по фону закрывал диалог, из-за чего выделение текста мышью внутри поля, если отпустить кнопку мыши уже за пределами окна, интерпретировалось как клик снаружи и закрывало форму.
- **Новое: прослушивание канала на аудиовыход.** В общих настройках приложения — выбор устройства воспроизведения (список через NAudio/WASAPI, с кнопкой обновления, аналогично списку устройств записи). На карточке канала — кнопка ▶/⏹ рядом со Старт/Стоп: воспроизводит живое аудио запущенного канала на выбранный аудиовыход независимо от того, идёт ли запись на диск. Одновременно играет только один канал. Новый `Services/AudioPlaybackService.cs` подписывается на аудио канала через `AudioEngineManager.SubscribeAudio` (новый проброс к `ChannelEngine`/`IAudioCapture`) и пишет его через `NAudio.Wave.WasapiOut`/`BufferedWaveProvider`.

## 0.00.018 — захват реального stderr ffmpeg при падении сетевого потока

- Расследовали жалобу на постоянные падения `ffmpeg` для канала «Авторадио» (HLS) — не удалось воспроизвести сбой напрямую (стрим сейчас воспроизводится нормально, только одно неопасное предупреждение `Packet corrupt`); похоже на временную проблему на стороне источника потока ~2 часа назад, а не на баг в коде. Заодно выяснилось: проблема с недоступностью устройств звуковой карты в том же логе объясняется тем, что приложение в тот момент выполнялось в сессии Remote Desktop — RDP-сессии по умолчанию не пробрасывают устройства записи звука. Это не баг приложения, действий не требуется.
- Нашли реальный пробел, из-за которого причину падения `ffmpeg` для сетевых потоков вообще нельзя было увидеть в логе: `StreamCapture` писал stderr `ffmpeg` только на уровне `Debug` построчно в реальном времени и не сохранял его — при падении процесса в лог попадал только код возврата, без единой строки о причине. У `AudioWriter` для процесса записи такая защита уже была (буфер stderr, показывается при падении), а у `StreamCapture` для процесса чтения — нет. Добавили тот же буфер (до 16 КБ) и вывод его содержимого в `LogWarning` вместе с кодом возврата при ненулевом коде выхода — при следующем падении в логе будет видна настоящая причина, а не только код.

## 0.00.017 — переход захвата со звуковой карты с BASS на NAudio

- **`SoundcardCapture` полностью переписан с BASS (`ManagedBass`) на [NAudio](https://github.com/naudio/NAudio) (MIT)** — снимает лицензионный вопрос BASS (свободно только для некоммерческого использования) полностью и без оговорок: NAudio бесплатна для любого использования, включая коммерческое.
- Заодно устранён целый класс багов, а не очередной точечный патч: три предыдущих раунда фиксов ложных разрывов соединения со звуковой картой (различение `Stopped`/`Stalled`, повторная проверка через 300 мс, диагностика видимости устройства при коде ошибки `Handle`) были реактивными обходами вокруг того, что BASS не давал ничего, кроме опроса `ChannelIsActive` каждые 500 мс. NAudio's `WasapiCapture` — событийная модель: `RecordingStopped` стреляет ровно при реальном завершении потока с точной причиной в `Exception`, `DataAvailable` — при поступлении данных. Опрос и все таймеры-дебаунсеры убраны за ненадобностью.
- Список устройств (`EnumerateDevices()`) теперь строится через `MMDeviceEnumerator.EnumerateAudioEndPoints(DataFlow.Capture, ...)` — loopback/render-устройства структурно не попадают в эту категорию Windows Core Audio API, поэтому реактивный фильтр `IsLoopback` (введённый в 0.00.014) не портируется, а просто удаляется — фильтровать больше нечего. Проверено на этой машине: список входов идентичен тому, что выдавал BASS (те же 6 реальных устройств), а формат UID (`{0.0.1.00000000}.{guid}`) совпадает с прежним 1-в-1 — уже сохранённые в конфигах `device_uid` продолжают резолвиться без изменений.
- `bass.dll` и пакет `ManagedBass` удалены полностью: из `Quince.Service.csproj`, из `Quince.Service/native/` (папка удалена), плюс убраны бесхозные копии `bass.dll`/`bassmix.dll` в корне репозитория, оставшиеся от более раннего удаления `ManagedBass.Mix`.
- `BassNativeLibraryTests.cs` удалён (проверять больше нечего — NAudio не грузит отдельную нативную библиотеку). `SoundcardCaptureTests.cs` (14 тестов на `ResolveDeviceIndex`) не потребовал изменений логики — только механическое переименование поля `Driver`→`Id`.
- README: убран раздел про лицензию BASS и таблица «Сторонние бинарные компоненты» сокращена до `ffmpeg.exe`/`ffprobe.exe` (у NAudio нет отдельного нативного файла для документирования).
- **Важно**: у нас нет реального проблемного виртуального аудиокабеля (VB-Audio Virtual Cable/Breakaway Pipeline) в этой среде для сквозной проверки — устранение самого КЛАССА ложных разрывов подтверждено на уровне архитектуры (нет больше опроса и гадания по коду ошибки), но реальную стабильность на длительной записи может подтвердить только пользователь на своей машине.

## 0.00.016 — диагностика повторного разрыва soundcard-подключения (код ошибки Handle)

- На новом сообщении о ложном разрыве звуковой карты (`Line 1 (Virtual Audio Cable)`, код ошибки BASS `Handle` вместо ранее виденных `Stopped`/`Stalled`) выяснилось: этот класс ошибки означает, что дескриптор записи стал НАСТОЯЩИМ невалидным (не временная пауза) — предыдущие исправления (различение `Stopped`/`Stalled`, повторная проверка через 300 мс) тут принципиально не помогают, т.к. дескриптор уже мёртв в любом случае, и переподключение — единственный доступный вариант восстановления. По логу видно, что переподключение действительно срабатывает («Переподключение к аудиоустройству выполнено»), просто повторяется часто — похоже на особенность драйвера конкретного виртуального аудиокабеля (сбрасывает pin захвата при простое/переконфигурации), а не на баг в коде.
- Добавлена диагностика: в предупреждение о разрыве добавлено, видно ли устройство в системе прямо в этот момент (`SoundcardCapture.EnumerateDevices()` вызывается заново) — это разграничит на будущее «устройство реально пропало» и «дескриптор умер, а устройство никуда не делось», если проблема повторится снова.

## 0.00.015 — исправлен формат «как во входном потоке», индикаторы метаданных/тишины, устойчивость обнаружения HLS-метаданных

- **Исправлен режим вывода «Как во входном потоке» (`output_format.mode: original`)**: раньше он ничего не делал, кроме отключения ручного пересэмплирования — файл всё равно сохранялся в том `file_format`, что был задан (по умолчанию `mp3`), поэтому HLS/AAC-канал реально писался в MP3. Теперь для этого режима кодек/расширение выбираются автоматически по источнику: `soundcard` → WAV PCM (частота/каналы и так уже брались из входного захвата, не менялись), `hls` → AAC, `icecast`/`icecast_mp3` → MP3. Диалог редактирования канала показывает подсказку «Будет сохранено как: …» под выбором режима; строка «Файл:» на карточке канала тоже теперь показывает реальный итоговый формат, а не сохранённое в конфиге значение.
- **Кнопка «Определить» (метаданные) больше не затирает уже заполненное поле пустым результатом** — раньше неудачная/не успевшая проверка (таймаут, временная сетевая ошибка) молча стирала рабочий URL, который пользователь мог ввести вручную; теперь поле меняется только при успешном обнаружении, а при неудаче показывается причина (если она известна).
- **Обнаружение JSON-эндпоинта метаданных HLS теперь устойчиво к одиночному сетевому сбою**: раньше единственная неудачная попытка при старте канала (например, кратковременная проблема с сетью в момент одновременного старта нескольких каналов) навсегда переводила канал на резервный разбор ID3 через ffprobe на всю сессию, даже если сам JSON-эндпоинт был рабочим. Теперь до 3 попыток обнаружения с паузой 3 с перед резервным вариантом.
- На карточке канала добавлены два новых индикатора в строке заголовка: круглый индикатор метаданных (голубой — режим метаданных настроен, серый — нет; для источников типа «Поток») и живой индикатор наличия сигнала (зелёный — звук есть, красный — тишина, по мгновенному True Peak) — не зависит от отдельно настраиваемого детектора тишины канала (который по умолчанию выключен), поэтому работает всегда. У всех индикаторов и элементов главной страницы (фильтр, пункты бургер-меню, строки карточки канала) добавлены всплывающие подсказки.
- Проверен фильтр списка звуковых устройств (`SoundcardCapture.EnumerateDevices`, введён в 0.00.014) — на тестовой машине корректно исключает loopback-устройства, оставляя только настоящие входы; отдельно отмечено, что некоторые виртуальные аудиокабели (например VB-Audio Virtual Cable) называют свой вход словом «Output» в имени устройства («CABLE Output») — это не баг фильтра, такова схема именования самой программы виртуального кабеля.
- Положение статусной строки на главной странице (под полем фильтра, над списком каналов) проверено — уже соответствует нужному расположению, изменений не потребовалось.

## 0.00.014 — GPL v3, устойчивость soundcard-подключения, правки метаданных и индикаторов

- Добавлена `LICENSE.md` (GNU GPL v3) — Айва распространяется как бесплатное opensource-приложение; README обновлён, чтобы отразить, что это удовлетворяет бесплатный уровень лицензии BASS.
- `SoundcardCapture.EnumerateDevices()` теперь исключает WASAPI loopback-устройства (записи с выходов, которые BASS перечисляет вместе с настоящими входами) — в списке устройств канала остаются только настоящие аудиовходы.
- Исправлена повторяющаяся ложная переустановка соединения со звуковой картой (`Запись с устройства прекратилась неожиданно` сразу после успешного подключения): добавлен повторный проверочный опрос через 300 мс перед тем, как считать канал реально отключённым (одиночный `BASS_ACTIVE_STOPPED` мог быть кратковременным сбоем виртуального устройства), плюс код ошибки BASS в лог для диагностики.
- Исправлена кнопка «Определить наличие метаданных», зависавшая на «Проверка…»: добавлен внешний таймаут (12 с) вокруг вызова сервиса на случай, если внутренние таймауты почему-то не сработали, и исправлена реальная утечка исключения в `HlsMetadataReader.DiscoverMetadataUrlAsync` — при некорректном URL потока `new Uri(...)` внутри генератора `BuildCandidateUrls` бросало необработанное исключение мимо `try/catch` цикла (кандидаты теперь материализуются заранее).
- **Поле «URL метаданных / режим» снова редактируется вручную** (текстовый ввод), кнопка «Определить» подставляет в него результат автоматически — переделано после 0.00.011, где поле было убрано в пользу «только кнопка», как в легаси-диалоге; практика показала, что редактируемое поле всё-таки нужно.
- В карточке канала горизонтальный индикатор уровня перенесён из-под строк «Вход:»/«Файл:» — теперь сразу под именем канала и кнопками управления, а строки «Вход:»/«Файл:» ниже него.
- В панели индикаторов канала добавлена контрастная шкала от `-∞` до `0` LU слева от индикаторов (метки каждые 6 дБ/LU, точно по той же dB→% шкале, что и сами индикаторы, растягивается вместе с ними). Расстояние между индикаторами TP L и TP R уменьшено до 3px (остальные — как раньше).

## 0.00.013 — bass.dll и ffprobe.exe теперь бандлируются

- **`bass.dll` (un4seen Developments) теперь бандлируется** в `native/bass.dll` → копируется в корень публикации рядом с `Quince.Service.exe` (туда, где .NET/Windows ищут нативные библиотеки для P/Invoke) — каналы `source.type: soundcard` больше не падают с `DllNotFoundException` на чистой машине. Взят подлинный файл (Un4seen Developments, версия 2.4.18.3) — проверен через `FileVersionInfo`. **Важно**: у BASS проприетарная лицензия — бесплатно только для некоммерческого использования, для коммерческого нужна отдельная регистрация у un4seen (см. новый раздел README «Сторонние бинарные компоненты»); сама бандлировка не заменяет и не подтверждает эту регистрацию.
- Заодно выяснилось, что `bassmix.dll`/пакет `ManagedBass.Mix` не используются вообще нигде в коде (`SoundcardCapture` вызывает только базовые функции `Bass.*`) — убрал неиспользуемый `PackageReference` на `ManagedBass.Mix`.
- **`ffprobe.exe` теперь тоже бандлируется** в `Quince.Service/tools/` (та же сборка gyan.dev `essentials_build`, что и `ffmpeg.exe`) — резервный ID3-разбор HLS-метаданных (см. 0.00.011/0.00.012) больше не требует ничего вручную устанавливать.
- Добавлен регрессионный тест `BassNativeLibraryTests` — проверяет, что `bass.dll` реально загружается (`Bass.Version`), а не просто лежит рядом.
- README: новый раздел «Сторонние бинарные компоненты» — таблица что где лежит/откуда взято/какая лицензия, отдельно и заметно про необходимость коммерческой регистрации BASS. Убраны устаревшие пункты «известных ограничений» про небандлированные BASS/ffprobe.

## 0.00.012 — тише лог при отсутствующем ffprobe.exe

- Резервный ID3-разбор HLS-метаданных (когда JSON-эндпоинт не найден) при отсутствии `ffprobe.exe` в `Quince.Service/tools/` раньше падал с `Win32Exception` и печатал полный стек в лог на уровне `warn` при каждом старте канала — это ожидаемое, не бандлируемое по тем же причинам, что и BASS, отсутствие бинаря, а не ошибка. Теперь `HlsMetadataReader` сначала проверяет `File.Exists`, и если файла нет — пишет одну спокойную строку на уровне `info` («ffprobe.exe не найден — резервный разбор ID3-тегов недоступен») вместо стектрейса.

## 0.00.011 — метаданные по образцу Quince Legacy, системный диалог папки, разные мелкие правки

- **Метаданные полностью переделаны на алгоритм и формат хранения из легаси-версии** (десктопный Python/Qt-предшественник, `../quince/src/audio/metadata_*.py`) вместо собственной эвристики из 0.00.010:
  - `source.metadata_url` теперь не свободный URL, а режим: `"icy"` (ICY inline из Icecast-потока, регэксп `StreamTitle='...'`, переподключение с нарастающей паузой 1/2/4/8/16/30 с) или любое другое непустое значение (HLS: авто-поиск JSON-эндпоинта метаданных по 3 адресам, производным от URL плейлиста, опрос раз в 5 с, при неудаче — одна попытка ID3-тегов через `ffprobe.exe`) или `""` (метаданные не собираются). Значение выставляется только автоматически, кнопкой определения — не вручную.
  - Хранение — daily CSV (`{metadata_path либо {save_path}/meta}/{YYYY-MM-DD}.csv`, UTF-8 с BOM, колонки `EventTime, ElemName, ElemArtist, ElemClass, ElemLength`), с той же отложенной записью строки (длительность трека известна только когда придёт следующее событие) — вместо старого «просто перезаписываем текстовый файл текущим названием».
  - Новые классы: `Audio/IcecastMetadataReader.cs`, `Audio/HlsMetadataReader.cs`, `Audio/MetadataWriter.cs`, `Audio/MetadataEvent.cs`, общий `Audio/UserAgents.cs` (вынесен из `StreamCapture`). Старые `Services/StreamMetadataService.cs` и `Audio/MetadataPoller.cs` удалены, заменены на `Services/MetadataDetectionService.cs` (только для кнопки в форме — сама запись метаданных больше не идёт через сервис, а напрямую через читатели в `ChannelEngine`).
- Диалог редактирования канала: кнопка «Проверить наличие метаданных» с текстовым полем заменена на «Определить наличие метаданных» без поля — как в легаси, результат («✓ ICY (Icecast)» / «✓ JSON: {url}» / «✗ Метаданные не найдены» / «⚠ Введите URL потока») окрашен и сохраняется в `metadata_url` автоматически.
- Кнопка «Обзор…» (папка сохранения) теперь сначала пытается открыть системный диалог выбора папки Windows (`System.Windows.Forms.FolderBrowserDialog`, `UseWindowsForms` включён в csproj) на машине, где выполняется приложение; если процесс не интерактивен (служба Windows) или диалог не открылся — прежний встроенный браузер папок остаётся автоматическим резервным вариантом.
- Исправлена ложная переустановка соединения со звуковой картой: движок ошибочно считал канал отключённым при `BASS_ACTIVE_STALLED` (временное отсутствие данных, например на тихом виртуальном аудиокабеле), а не только при `BASS_ACTIVE_STOPPED` — вызывало необоснованные циклы «отключено → переподключение».
- Обновление списка звуковых устройств в диалоге редактирования канала больше не запускается вслепую при каждом открытии формы (что каждый раз дёргало BASS и на машине без `bass.dll` сразу показывало ошибку) — только при выборе типа источника «Звуковая карта»; ошибка `DllNotFoundException` теперь показывает понятное сообщение про отсутствующий/небандлируемый `bass.dll` вместо голого текста исключения.
- Кнопки «Обновить»/«Обзор…» рядом с полями формы теперь совпадают по высоте с самими полями (были на пару пикселей ниже).
- Убрано окно «Индикаторы уровня — все каналы» и пункты бургер-меню «Индикаторы уровней»/«Выход» (см. 0.00.010) — кнопка «Индикаторы» на карточке канала теперь с иконкой 📊 (было ▦), кнопка удаления канала выделена красным.

## 0.00.010 — получение метаданных потока, список аудиоустройств по UID, упрощение меню

- Убрано окно «Индикаторы уровня — все каналы» и связанные пункты бургер-меню «Индикаторы уровней» (панель индикаторов теперь открывается только с конкретной карточки канала) и «Выход» (оставлен только «Перезапуск приложения…»).
- Реализовано получение метаданных потока (название трека): новый `StreamMetadataService` — при заданном «URL метаданных» разбирает ответ как JSON (ищет поля `title`/`song`/`track`/`now_playing`/… рекурсивно по всему документу) либо как обычный текст; при отсутствии «URL метаданных» для Icecast/Icecast-MP3 запрашивает встроенные ICY-метаданные прямо из потока (`Icy-MetaData: 1`, разбор блока `StreamTitle='...'` по `icy-metaint`). Для HLS без отдельного URL метаданных явно сообщается, что это не поддерживается.
- Новый `Audio/MetadataPoller.cs` — пока канал идёт (`source.type: stream`), в фоне раз в 15 секунд опрашивает метаданные тем же механизмом и, если задано `metadata_path`, записывает туда текущее название трека (перезаписывает файл только при смене трека). Раньше поле `metadata_path` в конфиге ни на что не влияло.
- В диалоге редактирования канала добавлена кнопка **«Проверить наличие метаданных»** рядом с полем «URL метаданных» — сразу показывает результат проверки (найденное название трека или причину, почему не найдено), без сохранения канала и без ожидания следующего опроса.
- Для каналов со звуковой картой в диалоге редактирования добавлен **выпадающий список обнаруженных устройств записи** с кнопкой «🔄 Обновить» (список можно освежить без переоткрытия диалога — например, после подключения USB-микрофона). Выбор устройства из списка автоматически подставляет его UID и имя. Поле «Устройство (индекс)» убрано из формы — индекс устройства нестабилен при переподключении USB-устройств, вместо него используется UID (уникальный идентификатор драйвера), сопоставление по которому уже было приоритетным в движке записи.

## 0.00.009 — статусная строка, иконка приложения, доработка формы канала

- Под полем фильтра на главной странице добавлена статусная строка — показывает результат «Обновить конфигурацию» (сколько добавлено/изменено/удалено), «Запустить/остановить все каналы» (сколько запущено/остановлено) и уведомление о перезапуске/выходе. Закрывается крестиком.
- Пиковые максимумы (с сбросом по клику) добавлены и для LUFS M/S/I в панели индикаторов канала — раньше были только у True Peak L/R. Клик по любому максимуму сбрасывает все максимумы канала разом (как и раньше для L/R).
- Граница карточки канала сделана контрастнее (`#e5e7eb` → `#9ca3af`).
- Диалог редактирования канала: «Режим формата вывода» перенесён в начало блока формата; поля «Формат файла», «Битрейт»/«Разрядность», «Частота дискретизации», «Число каналов» теперь показываются только при выборе «Заданный ниже» (при «Как во входном потоке» блок вообще скрыт — этих настроек не требуется). Частота дискретизации и число каналов переведены со свободного ввода на списки (8000/11025/22050/32000/44100/48000/96000 Гц; Моно/Стерео). Дефолт `OutputFormat.SampleRate` в модели конфигурации изменён с 48000 на 44100 (соответствует фактической частоте всего конвейера).
- Приложению добавлена иконка `.exe` (`Quince.Service/icon.ico`, `ApplicationIcon` в csproj) — сгенерирована программно (GDI+) по мотивам существующей `wwwroot/icon.svg`, т.к. в окружении сборки не нашлось инструмента конвертации SVG→ICO.
- Конфиги станций в исходниках (`Quince.Service/config/*.yaml`) синхронизированы с последней опубликованной сборкой (`release/0.00.008/config/`) — раньше при каждой публикации в `release/<версия>/config/` попадала версия из исходников, которая могла разойтись с тем, что реально редактировалось через запущенное приложение.

## 0.00.008 — доработка панели индикаторов, пиковые максимумы, производительность

- В панели индикаторов канала подписи и значения (TP L/R, LUFS M/S/I) теперь расположены НАД шкалой, а не под ней.
- Для TP L и TP R добавлен пиковый максимум («макс: …») — держится с момента запуска канала (или последнего сброса), клик по значению сбрасывает его. Максимум в общем списке «по всем каналам» переведён на ту же логику (раньше был нерезетящийся серверный `TruePeakMaxDb`).
- При остановке записи текущие значения индикаторов (TP, LUFS) обнуляются (показывают «—»); пиковый максимум при этом не трогается — сбрасывается только явным кликом.
- Подписи «TP L»/«TP R»/«LUFS M/S/I»/«L»/«R» сделаны контрастнее (тёмно-серый, полужирный вместо светло-серого).
- **Производительность**: на нескольких одновременно пишущих каналах индикаторы подвисали/замирали. Причина — `LevelMeter` пушил обновление уровня 10 раз в секунду НА КАЖДЫЙ канал; при 5 каналах это 50 рендеров в секунду на одно SignalR-соединение (Blazor Server обрабатывает их последовательно на одном circuit), чего достаточно, чтобы очередь рендеринга начала отставать и визуально «замирать». Снижена частота обновления до 5 раз в секунду (по-прежнему выглядит «живым» для индикатора, но вдвое меньше нагрузки).

## 0.00.007 — критический фикс окна индикаторов, сегментированные индикаторы, обзор папки, подсказки

- **Исправлен критический баг 0.00.006**: панель индикаторов канала всегда показывала «канал не найден» — параметр `ChannelName` (тип `string`) передавался из `MainLayout.razor` в `IndicatorsPanel` без `@` (`ChannelName="UiState.IndicatorsChannelName"`), из-за чего Blazor подставлял туда буквальную строку `"UiState.IndicatorsChannelName"` вместо значения свойства (для параметров-компонентов нестрокового типа `@` не обязателен, а для строковых — обязателен, иначе это литерал). Исправлено (`ChannelName="@UiState.IndicatorsChannelName"`), заодно проверены все остальные передачи параметров-компонентов в проекте на этот же паттерн ошибки.
- Индикаторы уровня (полоски TP на карточке и в панели индикаторов) теперь не перекрашиваются целиком в один цвет по текущему уровню, а показывают сегменты зелёный/жёлтый/красный на границах, заданных в настройках (`zone_yellow_db`/`zone_red_db`) — как раньше был статичный градиент, только теперь границы сегментов реально соответствуют настройкам, а не фиксированной пропорции. Общая логика вынесена в `Services/MeterZoneStyle.cs`, используется и картой канала, и панелью индикаторов (там же теперь и вертикальные, и общий список по всем каналам).
- Панель индикаторов канала (▦ на карточке) теперь не перекрывает список каналов, а раздвигает страницу: список каналов и панель — соседние колонки (`display:flex`), панель прилипает (`position: sticky`) к правому краю на высоту окна. Общий вид «по всем каналам» (пункт меню) остался прежним модальным окном.
- Для поля «Папка сохранения» добавлена кнопка **Обзор…**, открывающая собственный (внутриприкладной) браузер папок по файловой системе сервера — настоящий системный диалог выбора папки здесь невозможен, т.к. приложение обычно работает как служба Windows без доступа к рабочему столу. Новый `FolderBrowserService` отдаёт список дисков/подпапок только для чтения.
- Уточнена/подтверждена проверка папки записи: как и раньше, отсутствующая папка создаётся автоматически при сохранении; сообщение об ошибке показывается, только если диск или сетевой путь реально недоступны (без изменений в логике — перепроверено и задокументировано по итогам вопроса пользователя).
- Битрейт MP3/AAC теперь выбирается из списка (32/48/64/96/128/256/320 кбит/с) вместо произвольного числа.
- Добавлены всплывающие подсказки (`title`) для всех полей диалога редактирования/создания канала.

## 0.00.006 — исправление индикаторов уровня, гониометр, проверка папки записи

- **Исправлен баг**: индикаторы уровня (полоски TP) на карточках каналов и в окне индикаторов не отображались (хотя числовые значения обновлялись) — причина в том, что ширина полосы вычислялась как `double` и подставлялась в CSS `style` без учёта культуры; на сервере с русской локалью `45.7` превращалось в `45,7`, что для CSS `width`/`height` является невалидным значением и браузер его отбрасывал. Все числа, попадающие в inline `style`, теперь форматируются через `CultureInfo.InvariantCulture`.
- Окно индикаторов (▦ на карточке канала) для одного канала переделано в панель, пристыкованную к правому краю экрана (от низа шапки до низа страницы), вместо модального окна по центру. Индикаторы True Peak L/R и LUFS M/S/I теперь вертикальные шкалы, растягивающиеся по высоте вместе с панелью. Общий вид «индикаторы по всем каналам» (пункт меню) остался прежним модальным списком.
- Добавлен гониометр (стереофазовый осциллограф) — квадратная область фиксированного размера внизу панели индикаторов, отрисовывается на `<canvas>` через небольшой JS-модуль (`wwwroot/goniometer.js`) по децимированным сырым сэмплам L/R, которые теперь передаются с движка записи через новое событие `AudioEngineManager.GoniometerUpdated` (~10 раз/сек, не более ~256 точек за раз).
- Диалог редактирования/создания канала перед сохранением теперь реально проверяет доступность папки записи (`Directory.CreateDirectory` с таймаутом 5 с) — если сетевой путь или диск недоступны, показывается сообщение об ошибке вместо сохранения нерабочего конфига.
- Сохранение канала обёрнуто в try/catch: если конфиг успел исчезнуть (например, удалён из другой вкладки, пока диалог был открыт), пользователь увидит сообщение об ошибке вместо необработанного исключения, которое могло уронить весь Blazor-circuit (и выглядеть как «диалог сам закрылся из-за обновления страницы»).
- **Исправлен независимо найденный критический баг для self-contained сборки (0.00.005)**: `WebApplication.CreateBuilder(args)` по умолчанию берёт корень контента (и, соответственно, `wwwroot`) от рабочей директории ПРОЦЕССА, а не от папки, где лежит сам `.exe` — `ConfigDir`/`LogDir` уже были защищены от этого через `AppContext.BaseDirectory` (`PathResolver`), а встроенный `wwwroot` — нет. Из-за этого при запуске не из папки проекта (в частности — как служба Windows, где рабочая директория по умолчанию `System32`) все статические файлы (`app.css`, `icon.svg`, шрифты, теперь и `goniometer.js`) отдавались бы с 404, и интерфейс выглядел бы полностью без стилей. Раньше это маскировалось тем, что тестирование всегда шло через `dotnet run` из папки проекта. Исправлено явным заданием `ContentRootPath = AppContext.BaseDirectory` в `Program.cs`. Проверено прямым запуском опубликованного `Quince.Service.exe` из посторонней рабочей директории — все статические файлы и главная страница отдаются корректно.

**Незакрытый вопрос**: пользователь также сообщил, что диалог настроек канала иногда самопроизвольно закрывается уже во время работы с ним — конкретную причину подтвердить не удалось (нет браузерного инструмента для интерактивной отладки в этом окружении). Добавленная защита try/catch устраняет один вероятный сценарий (гонка с удалением канала), но не обязательно единственный — см. диалог с пользователем в этой сессии.

## 0.00.005 — самодостаточная (self-contained) публикация

- `Quince.Service.csproj`: добавлены `RuntimeIdentifier=win-x64` и `SelfContained=true`. `dotnet publish` теперь кладёт в выходную папку весь нужный .NET-рантайм — пользователю целевой машины больше не нужно отдельно ставить ASP.NET Core Runtime. Плата за это — размер публикации вырос примерно до 200 МБ.
- Проверено прямым запуском опубликованного `Quince.Service.exe` с `DOTNET_ROOT`, указывающим в никуда (эмуляция машины без установленного .NET) — приложение стартует и отвечает на HTTP без ошибок разрешения рантайма.
- **Важное ограничение, не устранённое этим изменением**: собственно .NET-рантайм теперь встроен, но нативная библиотека `bass.dll` (и `bassmix.dll`) для пакета `ManagedBass`, используемого в `SoundcardCapture`, по-прежнему не бандлится — NuGet-пакет `ManagedBass`/`ManagedBass.Mix` содержит только managed-обёртку. На машине без предустановленного BASS каналы `source.type: soundcard` упадут с `DllNotFoundException` при старте записи. `ffmpeg.exe` (используется `stream`-каналами) уже бандлится в `tools/` и этой проблемы не имеет.

## 0.00.004 — редактирование/клонирование/удаление канала, меню, индикаторы, вход со звуковой карты

- Реализована полная мутация каналов: создание, редактирование, клонирование, удаление (`ChannelManager.Add/Update/Delete/Clone`), с YAML-файлами конфигов в `config/` через уже существовавший `YamlConfigLoader.Save()`. `AudioEngineManager` реагирует на изменения «на лету» — перезапускает движок при правках, переносит его на новый ключ при переименовании канала, останавливает при удалении.
- UI: кнопки ✎/⧉/✕ на карточке канала теперь рабочие (модальный диалог редактирования/создания, подтверждение удаления, мгновенное клонирование).
- Окно индикаторов (▦) реализовано как модальная панель — True Peak L/R и LUFS (M/S/I) по одному каналу или сразу по всем.
- Бургер-меню полностью довязано: новый канал, настройки приложения, обновить конфигурацию (пересканирование `config/`), перезапуск/выход приложения, старт/стоп записи всех каналов, индикаторы, показать README.md.
- Настройки приложения (`app.yaml`) редактируются через UI и применяются вживую без перезапуска: уровень логирования и срок хранения логов (новый `AppSettingsService` + `FileLoggerProvider.UpdateSettings`), а также пороги/цвета индикатора уровня (используются теперь и в карточке канала, и в окне индикаторов вместо старого статичного градиента).
- Реализован вход со звуковой карты (`source.type: soundcard`): новый `SoundcardCapture` на `ManagedBass` (пакет был в зависимостях с версии 0.00.001, не использовался) с выбором устройства по `device_uid`/`device_name`/`device_index` и реконнектом по той же схеме статусов, что и у потокового источника. `StreamCapture`/`SoundcardCapture` теперь реализуют общий интерфейс `IAudioCapture`, `ChannelEngine` выбирает нужный по `source.type`. Кнопка Старт/Стоп для таких каналов больше не задизейблена.
- README.md теперь копируется рядом со скомпилированным приложением и доступен из меню «Показать файл README.md».

**Известные ограничения**: вход со звуковой карты не проверялся на реальном оборудовании (только модульные тесты логики выбора устройства); метаданные потока (ICY/HLS — название трека) всё ещё не реализованы; папка `config/RP/` (~150 доп. станций) по-прежнему не читается рекурсивно.

## 0.00.003 — защита обработчиков событий в AudioEngineManager

- Исправлена критическая уязвимость: `PushLevel` и `PushStatus` теперь оборачивают вызовы событий в try/catch с логированием. Конкретный риск: если обработчик события в `ChannelCard` выбросит исключение (например, `InvokeAsync` на разорванном Blazor-circuit), исключение распространялось бы из `ChannelEngine.MonitorAsync` или `SilenceDetector.RunAsync`, молча убивая background-task этого канала навсегда без логирования. Теперь все исключения отловлены и залогированы.

## 0.00.002 — аудио-движок для stream-каналов

- Реализован полный конвейер записи для каналов `source.type: stream` (Icecast/HLS): `StreamCapture` (ffmpeg-подпроцесс, декодирование, реконнект), `AudioWriter` (ротация по временной сетке, retention), `LevelMeter` (True Peak + LUFS, EBU R128), `SilenceDetector`.
- Новый `AudioEngineManager` (`IHostedService`) управляет движком по каналам, авто-старт для `auto_start: true`.
- UI: кнопка Старт/Стоп теперь реально запускает/останавливает запись, статус-точки и индикатор уровня отражают реальное состояние — напрямую через in-process C#-события `AudioEngineManager` (`LevelUpdated`/`StatusUpdated`) на каждую `ChannelCard`, без отдельного SignalR-хаба: обновление UI обеспечивает штатный circuit самого Blazor Server. Для каналов `soundcard` кнопка задизейблена — этот тип источника ещё не реализован.
- Забандлен `ffmpeg.exe` в `Quince.Service/tools/` (официальная статическая сборка), путь настраивается через `FfmpegPath`.
- Добавлен проект `Quince.Service.Tests` (xUnit) с покрытием чисто логических частей движка.

**Известные ограничения**: звуковая карта (`source.type: soundcard`) и метаданные потока (ICY/HLS — название трека) всё ещё не реализованы; окно индикаторов (▦) и редактирование/клонирование/удаление канала остаются визуальными заглушками.

## 0.00.001 — первая версионированная сборка

- Первый рабочий каркас `Quince.Service` (ASP.NET Core Blazor Server + SignalR, Windows Service).
- Исправлены проблемы окружения: закреплены версии `ManagedBass`/`ManagedBass.Mix` (3.1.0), добавлен отсутствовавший `Pages/_Imports.razor`.
- Конфиги каналов: устранена коллизия папок `Config`/`config` на регистронезависимой ФС — исходники перенесены в `Configuration/`, реальные данные — в `Quince.Service/config/` (копируются в выходную папку сборки при билде).
- Путь к папке конфигов и логов теперь анкерится к папке скомпилированного приложения (`AppContext.BaseDirectory`), а не к рабочей директории процесса — одинаково работает при `dotnet run`, прямом запуске `.exe` и как установленная служба Windows.
- `ChannelConfig` полностью переписан под реальную схему YAML легаси-приложения (`source`, `input_format`, `output_format`, `silence_detector` и т.д.); файлы без `name` (например `app.yaml`) больше не грузятся как «пустой канал».
- Веб-интерфейс: карточки каналов, повторяющие визуальный язык легаси-приложения на PyQt6 (статус-точки, кнопки Старт/✎/⧉/▦/✕, строки «Вход:»/«Файл:», плейсхолдер индикатора уровня).
- Header с бургер-меню (структура и логика — по образцу `apricot2`), пункты меню перенесены из легаси `main_window.py`; полностью скопированы размеры/шрифты (self-hosted Nunito) apricot2, иконка и цветовая палитра — на основе `quince/assets/icon.svg` (светло-жёлтый).
- Логирование: `Configuration/AppConfig.cs` (модель `app.yaml`), `Services/FileLoggerProvider.cs`/`FileLogger.cs` — файловый провайдер `ILogger` с ежедневной ротацией (`log/YYYY-MM-DD.log`), очисткой по сроку хранения, фильтрацией по уровню и точным форматом строки `YYYY-MM-DD HH:MM:SS.mmm [LEVEL] [channel_name] сообщение`.
- Заведено версионирование (`VersionInfo.cs`), `HISTORY.md`, `CHANGELOG.md`, `README.md`, публикация сборки в `release/`.

**Известные ограничения**: движок записи аудио (`ManagedBass`) не реализован — кнопки Старт/Стоп и большинство пунктов меню визуальные; живые индикаторы уровня и события ротации/переподключения/детектора тишины в лог пока не пишутся, т.к. нет реального аудио-пайплайна.
